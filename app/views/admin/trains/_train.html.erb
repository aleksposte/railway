<%= link_to 'Главная', root_path %>
<!-- Безымянные partials -->
<tr>
  <td><%= train.number %></td>
  
  <td><%= link_to 'Показать', admin_train_path(train) %></td>
  <td><%= link_to 'Редактировать', edit_admin_train_path(train) %></td>
  <td><%= link_to 'Удалить', admin_train_path(train), method: :delete, data: { confirm: 'Точно удалить?' } %></td>
</tr>
