<%= link_to 'Главная', root_path %>

<p id="notice"><%= notice %></p>
<h1>Маршруты:</h1>

<table>
  <thead>
    <tr>
      <th>Список:</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @routes.each do |route| %>
      <tr>
        <td><%= route.title %></td>
        <td><%= link_to 'Показать', admin_route_path(route) %></td>
        <td><%= link_to 'Редактировать', edit_admin_route_path(route) %></td>
        <td><%= link_to 'Удалить', admin_route_path(route), method: :delete, data: { confirm: 'Уверены?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Новый маршрут', new_admin_route_path %><br>


